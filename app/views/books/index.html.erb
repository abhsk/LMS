<%= flash_message %>
<table>
    <tr>
      <th>Title</th>
    </tr>
  <% @books.each do |book|%>
      <tr>
        <%=content_tag :td, (link_to book.title, book_path(book.id)).html_safe%>
      </tr>
  <%-end%>
</table>
